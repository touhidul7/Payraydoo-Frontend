const nextConfig = {
  /* config options here */
    // output: "export",
    images: {
        domains: ['*'],
        unoptimized: true,
    },

    
};

export default nextConfig;




// /** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;
// /** @type {import('next').NextConfig} */